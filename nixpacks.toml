[phases.setup]
aptPkgs = ["libgomp1"]

[phases.install]
cmds = [
  "pip install -r requirements.txt",
  "python -c \"from fastembed import TextEmbedding; TextEmbedding('BAAI/bge-small-en-v1.5')\""
]

[start]
cmd = "python scripts/seed_db.py && uvicorn api.main:app --host 0.0.0.0 --port $PORT"
